##Spring Configuration
server:
  port: 8080
spring:
  webflux:
    base-path: /api/v1
  application:
    name: heroes
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
routes:
  heroes: "/heroes"
